pandas>=2.0,<3.0
openpyxl>=3.1,<4.0
streamlit>=1.35,<2.0
xlrd>=2.0.1,<3.0
selenium>=4.6,<5.0

# Chỉ cần khi dùng ocr_so_phau_thuat_google.py (OCR sổ phẫu thuật qua Google
# Document AI). Không dùng OCR có thể bỏ qua dòng này.
google-cloud-documentai>=2.30.0
